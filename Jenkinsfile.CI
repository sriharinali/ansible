pipeline {
    agent any

    stages {
        stage('Ansible Ping Test') {
            steps {
                sh '''
                    
                    ansible-playbook -i host.ini ping.yaml
                '''
            }
        }
    }    
}